<% if object.errors.any? %>
  <div class="ui negative message">
    <%= t ".form_contains" %> <%= pluralize object.errors.count, "error" %>
    <ul class="list">
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
